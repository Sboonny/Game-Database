=====
bg-color
====
    background-color: hsl(37, 72%, 62%);
    background-color: hsl(355, 67%, 38%);
====
fonts
====
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700&family=Lexend+Deca&family=Nunito:wght@300;400;500;700;900&display=swap');
====
searchbar
======
style the searchbar 
add icons to the search bar
pick a font

=====
home
=====
add shadow effect to game element
fix the font 
fix the image
====
interceptors
====
try to make platform.slug works
add mat-select (selectionChange)="searchGames(sort)"
add (click)="openGameDetails(game.id) to div.game in home.component.html
  <!-- <img
             *ngFor="let gamePlatform of game.parent_platforms"            
              class="game-platform"
              src="assets/images/platforms/{{ gamePlatform.platform.slug }}.svg"
              alt="{{ gamePlatform.platform.slug }}"
            /> -->
===
detail
===
position gauge in better place
align text in game content